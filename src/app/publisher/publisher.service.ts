import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class PublisherService {

  constructor() { }

  //addPolicy(PublisherPolicy, UserId) return confirm
  //getPolicyByUser(UserId) return PublishPolicy
  //getPolicyById(PolicyId) return PublishPolicy
  //applyRestrictions(RoutingObject) return RoutingObject
}
